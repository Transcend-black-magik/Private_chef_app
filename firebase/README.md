Place your native Firebase files here for the stable mobile auth setup:

- `firebase/google-services.json` for Android
- `firebase/GoogleService-Info.plist` for iOS

Once those files exist, `app.config.ts` will pick them up automatically.
